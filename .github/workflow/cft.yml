name: cft-deploy

on:
  push:
    branches:
      - feature/**
    pull_request:
      

jobs:
  cft-actions:
    name: cft-deploy
    runs-on: self-hosted

    steps:
    - name: checkout repository
      uses: actions/checkout@master

    - name: Deploy CFT to aws in us-east-1
      run: aws cloudformation deploy --template-file vpc.iac.json --stack-name vpc-stack --parameter-overrides KeyNmae=task2
